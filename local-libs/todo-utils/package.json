{
  "name": "todo-utils",
  "version": "1.0.0",
  "description": "Shared interfaces and methods between server and app environments.",
  "main": "dist/index",
  "types": "dist/index",
  "private": true,
  "scripts": {
    "build": "tsc && npm pack",
    "install-app": "cd ../../ && npm i local-libs/todo-utils/*.tgz",
    "install-functions": "cd ../../functions && mkdir -p src/todo-utils/ && mv ../local-libs/todo-utils/*.tgz src/todo-utils/ && npm i src/todo-utils/*.tgz",
    "deploy": "npm run build && npm run install-app && npm run install-functions"
    },
  "dependencies": {
    "date-fns": "^2.6.0"
  },
  "files": ["dist"]
}