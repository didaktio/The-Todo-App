{
  "name": "utils",
  "version": "1.1.1",
  "description": "Shared interfaces and methods between server and app environments.",
  "main": "index",
  "types": "index",
  "private": true,
  "scripts": {
    "build": "tsc && cp package.json dist && cd dist && npm pack && mv *.tgz ../utils.tgz",
    "installApp": "cd ../ && npm i ./utils/utils.tgz",
    "installFunctions": "cd ../functions && mkdir -p src/utils/ && cp ../utils/utils.tgz src/utils/ && npm i ./src/utils/*.tgz && rm package-lock.json",
    "deploy": "npm version patch && npm run build && npm run installApp && npm run installFunctions"
  },
  "dependencies": {
    "date-fns": "^2.8.0"
  }
}
